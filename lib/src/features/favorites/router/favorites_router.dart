import 'package:cryptocurrency_app/src/features/favorites/view/container/favorites_container.dart';
import 'package:flutter/material.dart';

class FavoritesRouter extends StatelessWidget {
  const FavoritesRouter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FavoritesContainer();
  }
}
